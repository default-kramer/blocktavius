<UserControl x:Class="Blocktavius.AppDQB2.LayeredImagesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Blocktavius.AppDQB2"
             xmlns:dd="urn:gong-wpf-dragdrop"
             xmlns:pg="clr-namespace:Xceed.Wpf.Toolkit.PropertyGrid;assembly=Xceed.Wpf.Toolkit"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <local:ZoomToCropControl Grid.Column="1">
            <local:ZoomToCropControl.ZoomableContent>
                <ItemsControl ItemsSource="{Binding Layers}">
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Control.HorizontalAlignment" Value="Left" />
                            <Setter Property="Control.VerticalAlignment" Value="Top" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Grid />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl Content="{Binding}">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                                <ContentControl.Resources>
                                    <DataTemplate DataType="{x:Type local:MinimapLayer}">
                                        <Image Source="{Binding MinimapImage}">
                                            <!-- Tilesheet has tiles at 16x16 but we need 8x8 to match the 1-pixel-per-XZ convention -->
                                            <Image.LayoutTransform>
                                                <ScaleTransform ScaleX="0.5" ScaleY="0.5"></ScaleTransform>
                                            </Image.LayoutTransform>
                                        </Image>
                                    </DataTemplate>
                                    <DataTemplate DataType="{x:Type local:ChunkGridLayer}">
                                        <Image Source="{Binding ChunkGridImage}"></Image>
                                    </DataTemplate>
                                    <DataTemplate DataType="{x:Type local:ExternalImageLayerVM}">
                                        <Image Source="{Binding Image.ImageSource}"></Image>
                                    </DataTemplate>
                                </ContentControl.Resources>
                            </ContentControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </local:ZoomToCropControl.ZoomableContent>
        </local:ZoomToCropControl>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <ListView x:Name="lvLayers" ItemsSource="{Binding Layers}" SelectedItem="{Binding SelectedLayer}"
                      dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" dd:DragDrop.DropHandler="{Binding}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsVisible}" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding LayerName}" VerticalAlignment="Center" Margin="5,0,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Grid.Row="1">
                <Button Click="ChooseImages_Click">Choose Images...</Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
