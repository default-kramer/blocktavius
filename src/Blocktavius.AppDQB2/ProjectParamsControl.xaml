<UserControl x:Class="Blocktavius.AppDQB2.ProjectParamsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Blocktavius.AppDQB2"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Project File:"></Label>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
            <TextBox IsReadOnly="True" MinWidth="300" Text="{Binding ProjectFilePathToDisplay, Mode=OneWay}"></TextBox>
        </StackPanel>
        <Label Grid.Row="1" Grid.Column="0" Content="STGDAT File:"></Label>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <TextBox IsReadOnly="True" MinWidth="300" Text="{Binding StgdatFilePath, Mode=OneWay}"></TextBox>
            <Button Command="{Binding ChooseStgdatFile}">Choose...</Button>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="12,6,0,0">
            <CheckBox Content="Include in preview?" IsChecked="{Binding IncludeStgdatInPreview}"></CheckBox>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="12,8,0,0">
            <Button Content="Edit Chunk Grid" Click="EditChunkGrid_Click"></Button>
        </StackPanel>
    </Grid>
</UserControl>